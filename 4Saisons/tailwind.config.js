/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,jsx}"], // Ensure paths are correct
  theme: {
    extend: {}, // Empty for now, no custom config
  },
  plugins: [], // No plugins for now
}
